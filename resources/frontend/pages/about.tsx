import * as React from 'react'

import { Button, Input } from '@/components/ui'

export default function about() {
  return (
    <React.Fragment>
      ini about
      <Button>test</Button>
      <Input />
    </React.Fragment>
  )
}